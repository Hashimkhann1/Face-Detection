Face Detection

A simple Face Detection App in Flutter using the camera and google_mlkit_face_detection packages.

This app can detect single or multiple faces through the camera.

Setup
The app is ready to run directly on both Android and iOS.
